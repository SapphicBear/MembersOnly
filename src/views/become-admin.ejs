<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("./partials/head.ejs", { title: title }); %>
</head>
<body>
    <div class="wrapper become-admin">
        <header>
            <h1>Become an Admin?</h1>
        </header>
    
        <main class="become-admin">
            <h2>The secret admin administration registration area!</h2>
            <p>Current User: <%= user.username %> </p>
            <form action="/become-admin" method="post" class="become-admin-form" novalidate>
                <div class="form-row">
                    <label for="isadmin">Become admin?</label>
                    <input type="checkbox" name="isadmin" id="isadmin">
                </div>
                <div class="form-row">
                    <label for="password">Secret Password?</label>
                    <input type="password" name="secret_password" id="secret_password" required />
                    <% if (locals.errors) { %>
                        <span class="error"><%= errors.msg %></span>
                    <% } %>
                </div>
                <button type="submit" class="button">Submit</button>
                <button type="button" class="button" onclick="location.href='/'">Cancel</button>
            </form>
        </main>
    
        <footer>
            <%- include("./partials/footer.ejs", { links: links }); %>
        </footer>
    </div>
</body>
</html>